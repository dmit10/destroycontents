A simple script that replaces all files in some svn dump
(except those which start with "link ", they could be symlinks) into
files of the same sizes but filled with '.'.

The script leaves file names and log messages as is.

The script requires this

http://search.cpan.org/~book/SVN-Dump-0.04/230

If you use Debian, you can simply run

aptitude install libsvn-dump-perl

Usage:

./destroycontents.pl231 input.dump > output.dump

